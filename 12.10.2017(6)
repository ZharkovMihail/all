#include <iostream> 
#include <stdio.h>
using namespace std;
int main() {
	int a, b, c, d, e, f, r, m, k,g;
	for (a = 0; a <= 9; a++) {
		for (b = 0; b <= 9; b++) {
			for (c = 0; c <= 9; c++) {
				for (d = 0; d <= 9; d++) {
					for (e = 0; e <= 9; e++) {
						for (f = 0; f <= 9; f++) {
							for (r = 0; r <= 9; r++) {
								for (m = 0; m <= 9; m++) {
									for (k = 0; k <= 9; k++) {
										for (g = 0; g <= 9; g++) {
											if (((((a * 10 + b) * 10 + c) * 10 + 5) * 10 + d) + ((((f * 10 + g) * 10 + e) * 10 + d) * 10 + r) == ((((a * 10 + m) * 10 + c) * 10 + 5) * 10 + k)) {
												cout << a << " " << b << " " << c << " " << d << " " << e << " " << f << " " << r << " " << m << " " << k << " " << g << endl;
											}
										}								
									}
								}
							}
						}
					}
				}
			}
		}
	}
	system("pause");
	return 0;
}
